PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
PREFIX rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#>

SELECT
  (COUNT(*) AS ?triples)
  (SUM(IF(?o = skos:Concept, 1, 0)) AS ?conceptCount)
WHERE {
  ?s ?p ?o .
}
